import propsConfig from './props';
import pluginsConfig from './plugins';
import moleculesConfig from './moluecules';

const config = {
  props: propsConfig,
  plugins: pluginsConfig,
  molecules: moleculesConfig,
  settings: {
    enableKeywordValues: true,
  }
};

export default config;
