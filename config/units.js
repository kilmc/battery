const unitsConfig = {
  '': 'baseline',
  'p': 'percent',
  'px': 'hardPixel',
  'vh': 'viewportHeight',
  'vw': 'viewportWidth'
};

export const units = {
  baseFontSize: 16,
  baselineUnit: 6,
  useRems: true,
};

export default unitsConfig;