import { UserPropConfig } from 'types/prop-config';

export const textAlign: UserPropConfig = {
  prop: 'text-align',
  propIdentifier: 'text',
  keywordSeparator: '-',
  keywordValues: {
    center: 'center',
  },
};
